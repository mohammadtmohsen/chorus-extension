# Strict isolation: a package may only import what it declares.
# This is what mechanically enforces the layering in plan §3.2 — without it,
# hoisting lets `orchestrator` import Electron by accident and nothing complains.
hoist=false
strict-peer-dependencies=false

# Electron's own postinstall needs to fetch its binary.
node-linker=isolated
